[project]
name = "henri"
version = "0.1.0"
description = "A pedagogical Claude Code clone"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34.0",      # AWS SDK for Bedrock
    "rich>=13.0.0",       # Terminal formatting
]

[project.scripts]
henri = "henri.cli:main"
